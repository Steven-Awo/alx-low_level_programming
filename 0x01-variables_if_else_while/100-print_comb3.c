#include<stdio.h>
/**
 * main - A program that prints lowercase & Uppercase alphabet
 *
 *Return: 0 (Success)
 */
int main(void)

{
	int x, y;

	for (x = '0'; x <= '9'; x++)
	{
	for (y = '0'; y <= '9'; y++)
	{
	if (x != d)
	{
	putchar(x);
	putchar(y);
	if (x == '9' && y == '9')
	continue;
	putchar(',');
	putchar(' ');
	}
	}
	}
	putchar('\n');
	return (0);

}
