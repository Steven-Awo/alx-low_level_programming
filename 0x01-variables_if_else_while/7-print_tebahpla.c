#include<stdio.h>
/**
 * main - A program that prints lowercase alphabet in reverserve
 *
 *Return: 0 (Success)
 */
int main(void)

{
	char x;

	for (x = 'z'; x <= 'a'; x--)
	putchar(x);
	putchar('\n');
	return (0);

}
