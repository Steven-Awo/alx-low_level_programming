#include "main.h"

/**
 * print_line - A program to check execute number 6 code
 * @n: number of repetition
 * Return: void
 */
void print_line(int n)

{

	int m;

	for (m = 0; m < n; m++)
	{
	if (m != n)
	{
	_putchar(95);
	}
	else if (m < 0)
	{
	_putchar('\n');
	}
	else
	{
	_putchar('\n');
	}
	}
}
