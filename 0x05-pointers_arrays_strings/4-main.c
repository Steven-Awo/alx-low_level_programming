#include "main.h"
#include <stdio.h>

/**
 * main - A program that checks the code
 *
 * Return: 0 (successful)
 */

int main(void)

{
	char *str;

	str = "I do not fear computers. I fear the lack of them - Isaac Asimov";
	print_rev(str);
	return (0);
}
