#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - A PROGRAM TO RETURN THE LENGTH OF A STRING
 * @s: the string to be checked
 * Return: 0 (successful)
 */
int _strlen(char *s)

{
	return (strlen(s));
}
